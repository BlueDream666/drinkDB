/* ============================================================
 *  饮库 DrinkDB —— 站点配置
 *  ------------------------------------------------------------
 *  ⚠️ 安全说明：本地版没有服务器，admin.html 的口令只是在浏览器里比对，
 *     它只能防止「误入」，**不是安全边界** —— 懂技术的人可以直接读源码。
 *     上线到公网前必须换成服务端鉴权（见 README「三、上线方案」）。
 * ============================================================ */
window.DRINKDB_CONFIG = {
  siteName: '饮库',
  siteNameEn: 'DrinkDB',
  /* 署名，会显示在站名后面 */
  author: '宿屿',
  authorRole: '整理',
  tagline: '200+ 款饮料，总有一瓶是你想喝的',

  /* 仅本地版使用；上线前请删掉或改成服务端校验 */
  adminPasscode: 'drinkdb-admin',

  /* 上线后把这里改成你的后端地址；留空则用 localStorage 本地模式 */
  apiBase: '',

  /* 数据版本（用于导出的文件名） */
  version: 'v1.1',

  /* 没有筛选时，两排缓慢平移的速度（像素/秒）。数值越小越慢。 */
  flowSpeed: 30,
};

window.ADMIN_PASSCODE = window.DRINKDB_CONFIG.adminPasscode;
