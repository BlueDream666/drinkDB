/* ============================================================
 *  饮库 DrinkDB —— 站点配置
 *  ------------------------------------------------------------
 *  ⚠️ 关于审核台的口令，先把话说清楚：
 *
 *  这里**不存口令明文**，只存「随机盐 + SHA-256 摘要」。看到这份源码的人
 *  反推不出你的口令，所以「只有你知道口令」这一条是成立的。
 *
 *  但要说实话：这仍然**不是安全边界**。因为整个站是纯静态的，懂技术的人
 *  可以直接改浏览器里的状态把审核台「解锁」，绕过口令。所以它的定位是
 *  「挡住随手点进来的人」，不是「防止有人蓄意入侵」。
 *
 *  想真正做到只有你能审，必须让服务器来判断（README 第五节）。在那之前，
 *  这个口令保护的东西其实也不敏感 —— 全部饮料数据本来就是对所有人公开的。
 *
 *  改口令：  node tools/set-password.js
 * ============================================================ */
window.DRINKDB_CONFIG = {
  siteName: '饮库',
  siteNameEn: 'DrinkDB',
  /* 署名，会显示在站名后面 */
  author: '宿屿',
  authorRole: '整理',
  tagline: '200+ 款饮料，总有一瓶是你想喝的',

  /* 审核台口令：随机盐 + SHA-256 摘要。改口令跑 node tools/set-password.js */
  adminSalt: '4445b77cd74ac707b974441266a9fdc8',
  adminHash: '58aa86c2f993571831c85a0defd0d4b7fce92e4a892b195a129b0375fde4b51b',

  /* 后端地址。留空 = 本地模式（点赞评论只有自己看得到）；
     填上 Worker 网址 = 云端同步（所有人共享）。 */
  apiBase: 'https://drinkdb.bluedream666.workers.dev',

  /* 数据版本（用于导出的文件名） */
  version: 'v1.3',

};
