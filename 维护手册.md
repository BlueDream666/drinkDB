# 饮库 —— 维护手册

> 这份是给你自己看的，不用管排版好不好看。访客看的是 [README.md](README.md)。

---

## 一、本地预览

**方式 A：直接双击 `index.html`**
已实测可用，不需要服务器、不需要联网。

**方式 B：起个本地服务器（行为与线上完全一致）**

```powershell
cd F:\饮料统计\ds
node serve.js          # 会打印本机地址和手机可访问的局域网地址
```

| 页面 | 说明 |
|---|---|
| `index.html` | 前台：浏览 / 筛选 / 对比 / 创新区 / 导出 / 提交 |
| `admin.html` | 后台：审核网友提交的饮料、评论、建议 |

---

## 二、当前线上架构（2026-09 已上线）

| 部分 | 在哪 | 地址 |
|---|---|---|
| **网站** | GitHub Pages（分支部署，`main` → `/(root)`） | <https://bluedream666.github.io/drinkDB/> |
| **后端** | Cloudflare Worker `drinkdb` | `https://drinkdb.bluedream666.workers.dev` |
| **数据库** | Cloudflare D1，库名 `drinkdb` | 三张表：`likes` / `comments` / `suggestions` |
| **管理口令** | Cloudflare Secret `ADMIN_TOKEN` | 只存在服务器上，源码里没有 |

**GitHub Pages 必须保留仓库根目录的 `.nojekyll`**（空文件）。
没有它，Jekyll 会跳过 `js/data/` 下所有 `_` 开头的文件（比如 `_schema.js`），
页面会显示 0 款饮料。这是上线时踩过的坑。

---

## 三、怎么推代码（重要）

**用 SSH，不要用 HTTPS。**

```powershell
cd F:\饮料统计\ds
git add -A
git commit -m "说明"
git push
```

**为什么不能用 HTTPS**：实测国内直连 github.com 时，
HTTPS 的 `push` 会在上传 pack 时被重置（连接建立、拿到 refs、然后断掉）。
`ls-remote` 能通，`push` 不通——不是慢，是被掐。折腾一整晚都推不上去，换成 SSH 之后 8 秒完成。

**SSH 是怎么配的：**

- 密钥：`~/.ssh/id_ed25519_github`（公钥已加到 GitHub 账号）
- `~/.ssh/config` 里加了这段（走 443 端口，因为 22 端口常被封）：

  ```
  Host github.com
    HostName ssh.github.com
    Port 443
    User git
    IdentityFile ~/.ssh/id_ed25519_github
    IdentitiesOnly yes
  ```

- 远端地址：`git@github.com:BlueDream666/drinkDB.git`

**备用脚本**（万一 SSH 出问题）：
- `tools/push-ssh.ps1` —— 走 SSH 推送，带重试和公钥引导
- `tools/keep-pushing.ps1` —— 走 HTTPS 反复重试（带硬超时，因为 git 自己不会超时，会一直挂）
- `tools/gh-proxy.js` —— 本地代理解析覆盖，把 `github.com` 指到能用的 IP

---

## 四、改数据

数据全在 `js/data/` 的五个分册里（总共 203 款）：

| 文件 | 内容 |
|---|---|
| `01-water-soda.js` | 水 26 + 碳酸 26 |
| `02-tea.js` | 无糖茶 20 + 含糖茶 16 |
| `03-milk-coffee.js` | 奶 9 + 乳饮 14 + 咖啡 12 + 奶茶 12 |
| `04-juice-sport.js` | 果汁 16 + 功能 11 + 运动 10 |
| `05-alcohol-other.js` | 酒 20 + 其他 11 |
| `_schema.js` | 数据字典与装配器（**字段说明都在这**） |

新增一款：在对应分册末尾加一行 `B({...})`，`id` 不能重复，然后跑校验：

```powershell
node tools/check-data.js
```

### 字段速查

| 字段 | 含义 | 备注 |
|---|---|---|
| `sg` | 含糖 g/100ml | `0` = 标称无糖 |
| `s` | **糖分星级** 0–5（半星） | 「含多少糖」 |
| `sw` | **甜度星级** 0–5（半星） | 「喝起来多甜」——和糖分是两件事 |
| `sub` / `at` | 代糖类型 / 代糖后味 0–5 | 阿斯巴甜、三氯蔗糖、赤藓糖醇、甜菊糖苷… |
| `cb` | **气泡强度** 0–5 | `0` = 非碳酸 |
| `fd` | **放一天后剩余气泡** 0–5 | 原创指标 |
| `flatLoss` | 自动派生 = `cb - fd` | 掉气幅度，越大越不经放 |
| `ice` | 冰镇更好喝 / 常温也行 / 冰了反而差 | |
| `abv` `cf` `tea` `dairy` `hfcs` | 酒精%、咖啡因 mg/100ml、含茶、含奶、含果葡糖浆 | |
| `ml` `pk` `p` | 容量 / 包装 / 参考价 | |
| `pop` `ph` | 大众化星级 / 酸感星级 | |

---

## 五、界面逻辑（改代码前先看这段）

### 三个视图

顶部切换：**挑饮料 / 对比 / 创新区**。视图会写进网址，可以直接分享：

```
#view=compare&cmp=coke-classic,coke-zero,pepsi-zero
#view=lab
```

### 两排横滑（没筛选的时候）

没选任何条件、没搜索、没排序时，全部饮料分两排显示，**左右划着看**，不自动滚。

**为什么不做自动轮播**：做过一版，JS 逐帧平移。卡片一多（两排合计 400 多个 `<img>`）
就卡成幻灯片，手机上尤其明显。中途还踩了两个坑：CSS animation 会被系统的
「减少动态效果」直接关掉；暂停按钮如果顺带重新渲染，位置会清零跳回开头。
结论很简单——这种浏览场景本来就不需要动，交给浏览器原生滚动最省事也最顺。

### 预设（场景筛选）

筛选行是**属性视角**，预设是**生活视角**。点一下应用，再点一下取消。

> 预设不是「筛选按钮的组合」，它直接写在 `js/presets.js` 里当判定函数用，
> 所以能表达筛选行表达不了的东西（酸感、产地、整瓶含糖量）。

`js/presets.js` 里还有 `ORIGIN` 表（品牌 → 产地）和 `sugarCubes()`（方糖换算），
创新区的「中国汽水地图」和「方糖可视化」都用它。

### 三态筛选

**点一下 = 要（蓝），点两下 = 不要（红），点三下 = 取消。**

- 同一行里：蓝色的之间是「或」，红色的之间也是「或」，两组再相减；
- 不同行之间：都得满足；
- 选中的条件变成一排小标签显示在工具条下面，点 ✕ 就能去掉。

筛选状态写进网址，可以直接分享：

```
#f=carb:0,sugar:0,c:3!&sort=sugar-asc&q=可乐
```

（`!` 结尾表示"不要"。上例 = 没气的 + 糖很少的 + 不要茶类。）

### 筛选行

哪一类 / 糖 / 甜度 / 气 / 冰镇 / 咖啡因 / 茶 / 奶 / 酒精 / 价格 / 好买吗 / **预设** / **抽签**

排序有 14 种，含糖分升降、甜度升降、气最冲的在前、**最经放的在前**、咖啡因、度数等。

### 点赞 / 评论 / 建议

两种运行模式，页面上会标出来：

| 模式 | 数据存在哪 | 别人能看到吗 |
|---|---|---|
| 仅本机可见 | 浏览器 localStorage | 不能 |
| **云端同步**（现在） | Cloudflare D1 | 能，所有人共用一份 |

切换只靠 `js/config.js` 里的 `apiBase` 一个字段，前台代码一行都不用改。

---

## 六、后台

<https://bluedream666.github.io/drinkDB/admin.html>

**登录用 Cloudflare 上设的 `ADMIN_TOKEN`**（不是本地那个口令）。

登录后能看到：
- **网友补充的饮料** —— 通过后并入前台库并计入导出
- **待审评论** —— 网友在详情页发的评论，放行后才显示
- **待处理建议** —— 创新区「说点什么」提交的内容

### 本地模式的口令（没配后端时用）

源码里不存明文，只存「随机盐 + SHA-256 摘要」。改口令：

```powershell
node tools/set-password.js
```

跑完把 `js/config.js` 传上去就生效。忘了口令就再跑一次重设。

> 说明：本地校验仍然可以被绕过（懂技术的人能直接改浏览器状态）。
> 但配上 Worker 之后用的是 `ADMIN_TOKEN`，那个只存在服务器上，是真隔离。

---

## 七、图片

**153 款有真实产品图**（长边 360px 的 JPEG，合计 3.1 MB）。
其余是「泛指 / 自制 / 玩梗」条目，用 `js/art.js` 现场生成的矢量图。

优先级（`js/art.js`）：

1. 数据里写了 `img`（外部图片地址）→ 用外部图
2. `img/manifest.js` 里登记了本地图 → 用本地图（会依次试 `img/` 和 `assets/img/`）
3. 都没有 → 现场画一张矢量图

**手动补图**：把图片存成 `img/<饮料id>.jpg`，再跑 `node tools/shrink-images.js` 会自动登记。

**重新抓图：**

```powershell
node tools/fetch-images.js                          # 抓还没图的
node tools/fetch-images.js --only coke-classic --redo --q "可口可乐 330ml 罐"
node tools/shrink-images.js                         # 压到长边 360px
node tools/image-report.js                          # 看覆盖率、体积
```

**为什么压这么小**：原图平均 158 KB，总体上要下 60 MB。压到长边 360px 之后
23.5 MB → 3.1 MB，手机上就没压力了。

> 抓来的多是天猫 / 京东官方旗舰店的商品图。本库是非商业的个人整理，
> 但这不构成法律意见——要商用请自行确认图片授权。
> 想全部撤掉：删掉 `img/*.jpg` 和 `manifest.js`，页面自动回退成矢量图，不会坏。

---

## 八、导出

三个按钮，所有访客都能用：

| 按钮 | 内容 |
|---|---|
| **导全库** | 全部 203+ 款，28 列全特征，`.xls` 双击用 Excel / WPS 打开 |
| 导出筛选结果 | 当前筛选/搜索/排序后的子集 |
| CSV | 同样的 28 列，UTF-8 BOM，通用性最好 |

导出列：类别、产品名、品牌、英文别名、含糖 g/100ml、糖分星级、甜度星级、代糖类型、
代糖后味、热量、是否碳酸、气泡强度、放一天后气泡、开盖一天掉气、冰镇、酒精、咖啡因、
含茶、含奶、含果葡糖浆、常规容量、包装、参考价、大众化、酸感、特征小结、备注、数据来源。

实现是「HTML 表格 + Excel MIME」，不需要第三方库，离线也能用。

---

## 九、目录结构

```
ds/
├── index.html              前台（三个视图）
├── admin.html              后台
├── selftest.html           基础自测（51 项）
├── selftest-features.html  新功能自测（40 项）
├── serve.js                本地预览服务器
├── README.md               给访客看的
├── 维护手册.md               ← 你正在看的这份
├── 如何上线与注册域名.md      上线步骤与踩过的坑
├── 饮库-单文件版.html        打包成一个可以单独发出去的 html（4.3 MB，不进 git）
├── .nojekyll               ⚠️ 别删，删了 Pages 会白屏
├── css/style.css
├── js/
│   ├── config.js           站点名 / 署名 / 口令摘要 / apiBase
│   ├── sha256.js           纯 JS SHA-256
│   ├── art.js              矢量饮料图 + 图片路径解析
│   ├── store.js            存储层（LocalAdapter / RemoteAdapter）
│   ├── user.js             点赞 / 评论 / 建议
│   ├── presets.js          场景预设 + 产地 + 方糖换算
│   ├── compare.js          对比视图
│   ├── lab.js              创新区（抽签机 / 汽水地图 / 方糖）
│   ├── export.js           Excel / CSV 导出
│   ├── app.js              前台逻辑
│   ├── admin.js            后台逻辑
│   └── data/               五个分册 + _schema.js
├── img/                    153 张产品图 + manifest.js
├── worker/                 Cloudflare Worker + D1
│   ├── index.js            接口实现
│   ├── schema.sql          建表语句（注意：不能有 -- 注释）
│   ├── wrangler.toml       已填好 database_id
│   └── README.md           部署步骤（含「怎么注册 Cloudflare」）
└── tools/
    ├── check-data.js       数据自检
    ├── check-syntax.js     全部脚本语法检查
    ├── check-anchors.js    脚本引用的 DOM id 是否都有出处
    ├── fetch-images.js     抓产品图
    ├── shrink-images.js    压产品图
    ├── image-report.js     图片报告
    ├── set-password.js     设置本地审核口令
    ├── build-single.js     打包单文件版
    ├── deploy-worker.ps1   部署 Worker（登录→部署→设口令）
    ├── push-ssh.ps1        SSH 推送（推荐）
    ├── keep-pushing.ps1    HTTPS 反复重试推送（备用）
    ├── gh-proxy.js         本地代理解析覆盖（备用）
    └── push-now.ps1        一键推送（旧版，保留）
```

---

## 十、日常维护清单

```powershell
node tools/check-data.js      # 改完数据跑
node tools/check-syntax.js    # 改完脚本跑
node tools/check-anchors.js   # 改了 DOM id 跑
```

浏览器打开 `selftest.html` 和 `selftest-features.html`，各跑一遍（共 91 项）。

改完想给手机看，重新打包单文件版：

```powershell
node tools/build-single.js    # 生成 饮库-单文件版.html，可以直接发微信
```

---

## 十一、Worker / D1 备忘

### 改 Worker 代码之后

```powershell
cd F:\饮料统计\ds
.\tools\deploy-worker.ps1        # 登录 → 部署 → 设口令
```

或手动：

```powershell
cd F:\饮料统计\ds\worker
npx wrangler deploy
```

### 接口

| 方法 | 路径 | 说明 | 要口令 |
|---|---|---|---|
| GET | `/api/state` | 全部点赞数 + 已通过的评论 | 否 |
| POST | `/api/like` | `{id, on}` | 否 |
| POST | `/api/comment` | `{id, body, by}` 进待审 | 否 |
| POST | `/api/suggest` | `{body, by}` 进待审 | 否 |
| POST | `/api/login` | `{token}` | 否 |
| GET | `/api/pending` | 待审队列 | **是** |
| POST | `/api/moderate` | `{kind, dbId, ok}` | **是** |
| GET | `/api/stats` | 统计 | **是** |

### 坑

**建表语句里不能写 `--` 注释。** D1 的控制台粘贴时会吃掉换行，
而 SQL 的 `--` 是「到本行结尾为止」，换行没了注释就会吞掉后面半句，
报 `incomplete input: SQLITE_ERROR`。所以 `schema.sql` 里一条注释都没有。

**`AUTOINCREMENT` 必须写成 `INTEGER PRIMARY KEY AUTOINCREMENT`**，中间不能加别的。

---

## 十二、待办 / 可以再做的

- [ ] **糖 × 甜 散点图** —— 把「糖和甜是两件事」这个核心洞察画成一张图
- [ ] **「评论区 vs 实测」对照** —— 手里有 B 站评论区的提名次数，和真实属性做比对，别处没有
- [ ] 给 Worker 绑自定义域名（`workers.dev` 在国内时通时不通）
- [ ] 社交预览图（GitHub Settings → Social preview）
- [ ] 相似推荐（「和它最像的 5 款」）

---

## 十三、数据来源与免责

起因是 B 站一条视频的评论区提问，收到两百多条回复，整理成了底库。

糖分、热量、价格为常见规格的参考值，不同批次、口味、地区可能不同，**请以实际包装标签为准**。
本库是信息整理工具，不构成健康或营养建议。
