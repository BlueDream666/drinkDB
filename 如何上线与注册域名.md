# 怎么把饮库放到网上，以及域名怎么注册

你截图那个页面（`dash.cloudflare.com`）就是入口，**不用再去找别的平台**。
下面按「先免费跑起来 → 再考虑买域名」的顺序写，跟着点就行。

---

## 第一步：先拿到一个免费网址（不用买域名，10 分钟）

这一步不需要域名，也不需要信用卡。Cloudflare 会白送你一个 `xxx.pages.dev` 的网址。

1. 在你现在这个 Cloudflare 页面，看左边的 **Compute（计算）** 或直接找 **Workers & Pages**；
   也可以在首页那三个大卡片里选**左边那张「Ship something new」**，点下面的 **Create app**。
2. 下一页选 **Pages** 标签 → **Upload assets**（上传静态资源）。
   （不要选 "Connect to Git"，那需要先有 GitHub 仓库，麻烦。）
3. **Project name** 里填：

   ```
   yinku
   ```

   这个名字会直接变成你的网址：**`https://yinku.pages.dev`**。
   如果提示被占用，就换 `yinku-cn`、`drinkdb`、`hejiuku` 之类。
   注意：项目名一旦创建**不能改**，想换名字只能删掉重建（工作量就是把文件再拖一次）。
4. 把 `F:\饮料统计\ds` 这**整个文件夹**拖进虚线框，或者点虚线框选文件夹。
   它会自动把 `index.html`、`css/`、`js/`、`admin.html` 都传上去。
5. 点 **Deploy**。等十几秒，出现 `https://yinku.pages.dev` —— **这就是你的网址了，已经在网上，手机能直接打开。**

> 到这一步其实就已经"上线"了。后面买域名只是把这个网址换成更好记的。

---

## 第二步：买域名（可选，一个更好记的名字）

### 要不要买？先想清楚

| 你的情况 | 建议 |
|---|---|
| 只是自己用、发给朋友看 | **不用买**，`yinku.pages.dev` 完全够，而且永久免费 |
| 想印在名片上、发到社交平台 | 建议买，一年几十块 |
| 想做成正经产品 | 买，并且顺便把 `admin.html` 的服务端鉴权做掉 |

### 去哪儿买

**方案 A：就在 Cloudflare 买（推荐，省事）**

你截图首页中间那张卡片就是 **Add a domain → Register a new domain or bring your own**。

1. 点它，在输入框里输入你想要的名字，比如 `yinku`；
2. 它会列出一堆后缀和价格，常见的大概是：

   | 后缀 | 参考年费 | 说明 |
   |---|---|---|
   | `.com` | 约 ¥70–80 | 最通用，但好名字基本被抢光了 |
   | `.app` | 约 ¥80–90 | 必须 HTTPS，我们本来就是 HTTPS，合适 |
   | `.dev` | 约 ¥80–90 | 同上，程序员味重一点 |
   | `.fun` | 约 ¥20–30 | 便宜，适合个人小站 |
   | `.cn` | 约 ¥25–35 | 便宜，但见下面的注意事项 |

3. 选中后缀 → 一路 Next → 填联系信息 → **付款（需要信用卡 / PayPal）**。
4. 买完它自动就在你的 Cloudflare 账号里，DNS 也配好了，**不用再做任何解析设置**。

> Cloudflare 是按**成本价**卖域名的，不赚差价，所以通常比国内平台便宜一点点，
> 而且默认带免费 DNS、免费 HTTPS、免费防 DDoS。

**方案 B：在国内平台买（阿里云 / 腾讯云）**

- 好处：能用支付宝/微信付款，中文界面。
- 坑：`.cn` 域名必须做**实名认证**（传身份证，1–3 天）；如果不实名，域名会被暂停解析。
- 如果你想用国内服务器，还涉及 **ICP 备案**（要几周）。
  但**如果我们把页面放在 Cloudflare / GitHub Pages 这类海外服务上，是不需要备案的**。
- 买完要在阿里云后台改 DNS，把 nameserver 指向 Cloudflare 给你的两条地址，
  或者直接在 Pages 项目里绑定时按提示加一条 CNAME 记录。

### 我们的项目建议用哪个

```
yinku.app        首选，短、好念、跟「饮库」对得上
yinku.fun        便宜，气质轻松，适合这种好玩的小项目
drinkdb.com      英文向，但 .com 好名字基本没了
yinku.cn         便宜，但要做实名认证
```

我个人会挑 **`yinku.app`**——字数少，拼音和英文都念得顺，一年也就一杯咖啡的钱。

---

## 第三步：把买好的域名绑到 Pages

1. 回到 **Workers & Pages** → 点进你刚建的项目 → **Custom domains** 标签；
2. 点 **Set up a custom domain**，输入你买的域名，比如 `yinku.app`；
3. 如果域名就是在 Cloudflare 买的，**它会自动帮你加好解析记录**，点确认就行；
4. 等 1–5 分钟，状态变成 **Active**，即可用 `https://yinku.app` 打开。
   HTTPS 证书也是自动签发的，不用管。

顺便建议把 `www.yinku.app` 也加上，重定向到主域名。

---

## 第四步：想真正「只有我能看后台」，还差这一步

**这一步不做，`admin.html` 就只是个摆设。**

现在的 `admin.html` 口令写在 `js/config.js` 里，浏览器端比对。
任何人在电脑上按 F12 打开源码，就能看到口令——**这不是安全，只是挡误点**。

而且还有更根本的问题：现在网友提交存在**他自己浏览器的 localStorage 里**，
你在自己电脑上根本看不到。也就是说，「传输到我的后台」这件事，**本地版做不到**。

要真做到，需要三样东西：

| 需要什么 | 用什么 | 要花钱吗 |
|---|---|---|
| 一个能收提交的接口 | Cloudflare Pages Functions | 免费（10 万次/天） |
| 一个能存数据的库 | Cloudflare D1（SQLite） | 免费（5GB） |
| 一个只有你能过的登录 | Cloudflare Access（邮箱验证码） | 免费（50 人以内） |

代码里已经留好了接口，`js/store.js` 里的 `RemoteAdapter` 就是给它准备的：

```
GET    /api/beverages                  公开，返回所有已通过的饮料
POST   /api/submissions                任何人可提交
GET    /api/submissions?status=pending 待审列表（要管理员 token）
POST   /api/submissions/:id/approve    通过（要管理员 token）
POST   /api/submissions/:id/reject     退回（要管理员 token）
```

换的时候只要在 `js/config.js` 里填上 `apiBase`，再把 `Store.adapter` 指向 `RemoteAdapter`，
前台一行都不用改。

> 你说「检查通过之后再上线」，那这块我可以等你确认了再写。
> 到时候我按 Cloudflare 的免费方案把 `functions/` 那套接口和 D1 建表语句一起给你，
> 你只要在后台点几下、把 token 填进去就行。

---

## 常见问题

**Q：`pages.dev` 的免费网址会被墙吗？**
A：`*.pages.dev` 在国内时通时不通。如果主要给国内朋友看，建议买个域名绑上去，
或者干脆同时部署一份到 GitHub Pages 做备用。

**Q：一定需要备案吗？**
A：服务器放在海外（Cloudflare、GitHub Pages、Vercel）**不需要备案**。
只有用国内服务器（阿里云/腾讯云主机）才需要。

**Q：免费额度够用吗？**
A：Cloudflare Pages 是无限流量无限请求；Functions 每天 10 万次调用；D1 每天 500 万行读取。
你这种规模的站点，**远远用不完**。

**Q：以后想换平台麻烦吗？**
A：不麻烦。`ds` 是个纯静态文件夹，拖到 Netlify、Vercel、GitHub Pages 都能直接用。
唯一绑在 Cloudflare 上的是第四步的后端，那部分代码量不大，换起来也就一两个小时。
