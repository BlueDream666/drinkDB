# 怎么把饮库放到网上，以及域名怎么注册

## 先回答：白拿的网址能一直用吗？

**能，但两家情况不一样，值得分清楚。**

| | Cloudflare `xxx.pages.dev` | GitHub `xxx.github.io/仓库名` |
|---|---|---|
| 收费 | 永久免费，不是试用 | 永久免费 |
| 有效期 | 只要你不动它就一直有效 | 跟着账号，账号在就一直在 |
| 会不会被收走 | 项目删了名字会释放，别人可能抢 | 仓库名就是网址，别人抢不走 |
| 归谁 | 严格说是 Cloudflare 的子域，你只有使用权 | 同理，是 GitHub 的子域 |
| 国内访问 | 时通时不通 | 时通时不通（`github.io` 一样） |
| 缺点 | 名字长，不好记 | 网址里带用户名 |

**结论**：两个都能长期免费用，都不是"试用期"。但它们都是**别人家的域名**——
哪天平台改政策、或者你想换地方，网址就变了。

**想一劳永逸只有一个办法：自己买个域名。** 域名是你的财产，指向哪台服务器随时改，
换托管平台网址也不变。所以建议：

1. 先白拿一个（GitHub 或 Cloudflare），把东西放上去，先能用；
2. 如果确实想长期做，再花几十块买个域名指过去。

---

## 方案 A：GitHub Pages（跟你已登录的账号对上）

仓库 `github.com/BlueDream666/drinkdb` 现在还不存在。我这边已经把本地 git 仓库
初始化好、也提交了第一个 commit（182 个文件），就差推送这一步。

### ⚠️ 我这台机器上遇到的两个障碍

1. **`github.com` 时通时不通**。我连测 5 次全部超时，过一会儿再试又是 HTTP 200。
   国内直连 GitHub 就是这个样子。
2. **系统里存的 GitHub 凭据失效了**。我试探了一下，返回
   `Invalid username or token. Password authentication is not supported for Git operations.`，
   说明 Windows 凭据管理器里那条旧记录已经不能用了。

所以**推送这一步得你自己来**，我代替不了。下面是最省事的做法。

### 做法一：浏览器上传（不用命令行，推荐）

1. 打开 <https://github.com/new>
2. 仓库名填 **`drinkdb`**，选 **Public**，**不要**勾 "Add a README file"，点 **Create repository**
3. 新仓库页面点 **uploading an existing file**
4. 把 `F:\饮料统计\ds` 里这些**全部拖进去**（`assets` 有 153 张图约 3 MB，等一会儿）：

   ```
   index.html   admin.html   selftest.html
   css/   js/   assets/   .github/
   ```

   > `.github` 是隐藏文件夹。文件资源管理器里先打开「查看 → 显示 → 隐藏的项目」。

5. 点 **Commit changes**
6. 进仓库 **Settings → Pages**，把 **Source** 改成 **GitHub Actions**
7. 等一两分钟 Actions 跑完，网址就是：

   ```
   https://bluedream666.github.io/drinkdb/
   ```

> 第 6 步是关键。不选 GitHub Actions，我已经写好的
> `.github/workflows/pages.yml` 不会生效，页面打不开。

### 做法二：命令行推送

先把失效的旧凭据删掉，否则 git 会一直拿它去认证：

```powershell
cmdkey /delete:LegacyGeneric:target=git:https://github.com
```

然后在 `F:\饮料统计\ds` 下：

```powershell
.\tools\push-to-github.ps1 -Repo drinkdb
```

脚本会检查连通性、确认仓库建好、然后推送。第一次推会弹 GitHub 登录窗口
（Git Credential Manager 会打开浏览器让你授权），授权一次以后就记住了。

也可以手动来：

```powershell
cd F:\饮料统计\ds
git remote -v                      # 应该已经是 https://github.com/BlueDream666/drinkdb.git
git push -u origin main
```

**如果报 `Failed to connect to github.com`**：开代理后
`git config --global http.proxy http://127.0.0.1:7890`（端口换成你自己的），
或者改用 SSH，见 `tools\README-推送.md`。

---

## 方案 B：Cloudflare Pages（不用 GitHub，10 分钟）

你之前截图那个 `dash.cloudflare.com` 就是入口。

1. 首页左边那张 **Ship something new** → **Create app** → **Pages** 标签 → **Upload assets**
2. **Project name** 填 `yinku`，它就是网址 **`https://yinku.pages.dev`**
   （项目名建了不能改，想换只能删掉重建）
3. 把 `F:\饮料统计\ds` 整个文件夹拖进虚线框
4. 点 **Deploy**，等十几秒，网址就出来了

**好处**：不用 Git，拖一下就完事。**坏处**：以后每次改都要重新拖一遍，
不像 GitHub 那边 push 完自动更新。

> 两个平台可以同时用，互为备份。`ds` 是纯静态文件夹，丢哪都能跑。

---

## 第二步：买域名（可选，一个更好记的名字）

### 去哪儿买

**方案 A：就在 Cloudflare 买（推荐，省事）**

1. 点首页中间那张 **Add a domain → Register a new domain**；
2. 输入 `yinku`，它会列出后缀和价格：

   | 后缀 | 参考年费 | 说明 |
   |---|---|---|
   | `.com` | 约 ¥70–80 | 最通用，好名字基本抢光了 |
   | `.app` | 约 ¥80–90 | 强制 HTTPS，我们本来就是，合适 |
   | `.dev` | 约 ¥80–90 | 同上 |
   | `.fun` | 约 ¥20–30 | 便宜，适合个人小站 |
   | `.cn` | 约 ¥25–35 | 便宜，但见下面注意事项 |

3. 选中后缀 → 填联系信息 → 付款（要信用卡或 PayPal）；
4. 买完自动就在你账号里，DNS 也配好了，**不用再做任何解析设置**。

> Cloudflare 按**成本价**卖域名，不赚差价，通常比国内平台还便宜一点，
> 而且默认带免费 DNS、免费 HTTPS、免费防 DDoS。

**方案 B：国内平台（阿里云 / 腾讯云）**

- 好处：支付宝/微信付款，中文界面。
- 坑：`.cn` 必须**实名认证**（传身份证，1–3 天），不实名会被暂停解析。
- 买完要改 DNS，把 nameserver 指向 Cloudflare 给的两条地址，
  或者在绑定域名时按提示加 CNAME 记录。

### 建议

```
yinku.app        首选。短、好念、跟「饮库」对得上，一年约 ¥80
yinku.fun        便宜（¥20–30），气质也搭这种小项目
drinkdb.com      英文向，但好名字基本没了
yinku.cn         便宜，但要做实名认证
```

我会挑 **`yinku.app`**。

### 绑定到 GitHub Pages

1. 仓库 **Settings → Pages → Custom domain**，填 `yinku.app`，Save；
2. 去域名 DNS 加记录（Cloudflare 买的就在 DNS 页面加）：

   | 类型 | 名称 | 内容 |
   |---|---|---|
   | A | @ | 185.199.108.153 |
   | A | @ | 185.199.109.153 |
   | A | @ | 185.199.110.153 |
   | A | @ | 185.199.111.153 |
   | CNAME | www | bluedream666.github.io |

3. 等几分钟，勾上 **Enforce HTTPS**。

**绑定到 Cloudflare Pages** 更简单：项目 → **Custom domains** → Set up a custom domain，
域名要是在 Cloudflare 买的，解析会自动加好。

---

## 第三步：想真正「只有我能看后台」，还差这一步

**这一步不做，`admin.html` 就只是个摆设。**

现在口令写在 `js/config.js` 里，浏览器端比对。任何人按 F12 看源码就知道口令了。
更根本的问题是：网友的提交存在**他自己浏览器的 localStorage** 里，
你在自己电脑上根本看不到——「传输到我的后台」这件事，纯静态站点做不到。

要真做到，需要三样：

| 需要什么 | 用什么 | 花钱吗 |
|---|---|---|
| 能收提交的接口 | Cloudflare Pages Functions | 免费（10 万次/天） |
| 能存数据的库 | Cloudflare D1（SQLite） | 免费（5GB） |
| 只有你能过的登录 | Cloudflare Access（邮箱验证码） | 免费（50 人以内） |

接口契约已经留在 `js/store.js` 的 `RemoteAdapter` 里：

```
GET    /api/beverages                  公开，返回所有已收录的饮料
POST   /api/submissions                任何人可提交
GET    /api/submissions?status=pending 待审列表（要管理员 token）
POST   /api/submissions/:id/approve    通过（要管理员 token）
POST   /api/submissions/:id/reject     退回（要管理员 token）
```

切换只要在 `js/config.js` 填上 `apiBase`，再把 `Store.adapter` 指向 `RemoteAdapter`，
前台一行都不用改。等你确认要做，我把 `functions/` 那套接口和 D1 建表语句一起写好。

---

## 常见问题

**Q：GitHub Pages 一定要公开仓库吗？**
A：免费账号要。私有仓库想用 Pages 得升级付费。介意代码公开就用 Cloudflare Pages。

**Q：一定需要备案吗？**
A：服务器在海外（GitHub Pages、Cloudflare Pages、Vercel）**不需要备案**。
只有用国内服务器才需要。

**Q：免费额度够用吗？**
A：GitHub Pages 每月 100 GB 流量、软限制；Cloudflare Pages 无限流量。
你这种规模远远用不完。

**Q：以后想换平台麻烦吗？**
A：不麻烦。`ds` 是纯静态文件夹，所有路径都是相对的，丢到 Netlify、Vercel 都能直接跑。
唯一绑在 Cloudflare 上的是第三步的后端。
